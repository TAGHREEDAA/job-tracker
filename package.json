{
  "name": "job-tracker",
  "version": "1.0.0",
  "description": "Resume-aware remote backend and product job recommendations in Google Sheets",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test && node src/index.js --dry-run",
    "test:unit": "node --test"
  },
  "dependencies": {
    "googleapis": "^144.0.0",
    "node-fetch": "^3.3.2",
    "cheerio": "^1.0.0",
    "xml2js": "^0.6.2"
  },
  "engines": {
    "node": ">=20"
  }
}
